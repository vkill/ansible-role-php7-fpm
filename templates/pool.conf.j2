;{{ ansible_managed }}


[{{ item.name }}]
; Defaults
{% for directive, value in php7_fpm_pool_defaults.iteritems() %}
  {{ directive }} = {{ value }}
{% endfor %}

; Pool custom directives
{% for directive, value in item.iteritems() if directive != "name" %}
  {{ directive }} = {{ value }}
{% endfor %}